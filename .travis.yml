language: python
python:
  - "2.7"
  - "3.6"

# Credit: https://gist.github.com/petrbel/c3bf7e0f59c22a52f534
git:
  submodules: false

before_install:
  - sed -i 's/git@github.com:/https:\/\/github.com\//' .gitmodules
  - git submodule update --init --recursive

install:
  - pip install .

script:
  - python -c "import czifile"
